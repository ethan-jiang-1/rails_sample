<%= form_for @micropost do |f| %>
<%= render 'shared/error_messages', :object => f.object %>
<div class="field" rows="8">
<%= f.text_area :content, :rows=>4 %>
</div>
<div class="actions">
<%= f.submit "Submit" %>
</div>
<% end %>
